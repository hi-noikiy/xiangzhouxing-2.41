<!--pages/mine/donation/index.wxml-->
<view class="list">
  <view class="empty-wrapper">
    <image src="{{emptyIcon}}" class="empty-icon" />
    <view>{{text || '暂无内容' }}</view>
    <view wx:if="{{t2}}">{{ t2 }}</view>
  </view>
</view>
